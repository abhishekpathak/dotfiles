[core]
	excludesfile = ~/.gitignore
	autocrlf = input
[user]
    name = Abhishek Pathak
    email = me@abhishekpathak.net
[alias]
    co = checkout
[color]
        ui = auto
	branch = auto
	diff = auto
	status = auto
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = green ul
	frag = magenta bold
	old = red bold
	new = blue bold
[color "status"]
	added = yellow
	changed = green
	untracked = cyan
[credential]
    useHttpPath = true
	helper = osxkeychain
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
