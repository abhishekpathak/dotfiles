Here I store various configuration files to quickly set up the environment.
To start:

1. clone/download the repository to `$HOME/workspace/personal/dotfiles`
2. sync the folder `$HOME/workspace/store`.
3. run the bootstrap script: `/bin/bash $HOME/workspace/personal/dotfiles/configure.sh`
